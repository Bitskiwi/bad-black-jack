#ifndef KILLME_H
#define KILLME_H

// KEYWORDS

#define assuming if 
#define otherwise else 
#define during(x) while(x) 
#define dont_continue break
#define dont_break continue
#define iterate(x, y, z) for(x; y; z) 
#define preform do 
#define provide return 
#define compare(x) switch(x) 
#define wether(x) case(x) 
#define standard default 
#define structure struct
#define enumeration enum
#define define_type typedef

// TYPES

define_type int integer;
define_type char character;
define_type float floating_point_integer;

#endif
